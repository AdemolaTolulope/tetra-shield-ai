# models/
No fitted ML models are shipped: the decision engine is deterministic evidence integration
(docs/MODEL_CARD.md). Deterministic artifacts live in results/ (docking poses, identities,
Tanimoto). If a future dataset supports trustworthy ML, fitted artifacts land here with a
per-model card and leakage analysis.
